"""JAX-native, GPU-vectorizable reimplementation of the Balatro core game loop.

This package is under active development. Future tasks will add:
    CoreState, reset, step, batched_reset, batched_step
"""
